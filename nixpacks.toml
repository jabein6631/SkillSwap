# Nixpacks Configuration for Railway.app Deployment
[phases.setup]
nixPkgs = ["nodejs_20", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'SkillSwap build complete'"]

[start]
cmd = "node backend/database/seed.js && node backend/server.js"
